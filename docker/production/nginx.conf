server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    root /var/www/bagisto/public;
    index index.php;
    charset utf-8;

    client_max_body_size 100M;

    # Security headers (X-Frame-Options, Referrer-Policy, ...) are set by the
    # app's SecureHeaders middleware — do not also add them here, or every
    # response ships doubled, conflicting values.

    # --- Health check endpoint ---
    location = /health {
        access_log off;
        return 200 "OK";
        add_header Content-Type text/plain;
    }

    # --- Static assets with cache headers ---
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp|avif)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
        try_files $uri =404;
    }

    # --- Laravel routing ---
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }

    # robots.txt is served by the app (Shop SeoController, admin-configured).
    # Fall through to Laravel when no static file exists in public/.
    location = /robots.txt  {
        access_log off;
        log_not_found off;
        try_files $uri /index.php?$query_string;
    }

    error_page 404 /index.php;

    # --- PHP-FPM ---
    location ~ ^/index\.php(/|$) {
        fastcgi_pass unix:/run/php/php-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;

        fastcgi_buffering on;
        fastcgi_buffer_size 16k;
        fastcgi_buffers 16 16k;

        fastcgi_connect_timeout 60s;
        fastcgi_send_timeout 300s;
        fastcgi_read_timeout 300s;
    }

    # --- Deny dotfiles ---
    location ~ /\.(?!well-known).* {
        deny all;
    }

    # --- Deny access to sensitive files ---
    location ~ /\.(env|git|svn) {
        deny all;
        return 404;
    }

    # --- Gzip ---
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
}
